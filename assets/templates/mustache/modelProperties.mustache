const { Schema } = require("mongoose")
const ObjectId = Schema.Types.ObjectId

{{#withoutProperties}}
const {{ model }}ModelProperties = {
  // fields
}
{{/withoutProperties}}
{{#hasProperties}}
const {{ model }}ModelProperties = {
  {{#properties}}
    {{key}} : {
      {{> child}}
    },
  {{/properties}}
}
{{/hasProperties}}

module.exports = {{ model }}ModelProperties
