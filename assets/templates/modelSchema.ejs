const { Schema } = require("mongoose")
const ObjectId = Schema.Types.ObjectId

const <%= model %>SchemaProperties = {
  // fields
}

const <%= model %>Schema = new Schema(
  <%= model %>SchemaProperties,
  { timestamps: true }
)

module.exports = {
  schema: <%= model %>Schema,
  properties: <%= model %>SchemaProperties
}